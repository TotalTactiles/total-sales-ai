
// This file re-exports from the refactored auth provider for backward compatibility
export { AuthProvider, useAuth } from './AuthProvider';
export type { AuthContextType, Profile, Role } from './types';
