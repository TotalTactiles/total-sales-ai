
import React from 'react';
import RestoredManagerDashboard from './RestoredManagerDashboard';

const AIManagerDashboard: React.FC = () => {
  return <RestoredManagerDashboard />;
};

export default AIManagerDashboard;
